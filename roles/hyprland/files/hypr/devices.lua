-- devices.lua
-- Per-device input tweaks via hl.device() (separate from hl.config()).
-- Device names come from `hyprctl devices`.

hl.device({ name = "logitech-k400-plus-1", natural_scroll = true, scroll_factor = 0.5 })
