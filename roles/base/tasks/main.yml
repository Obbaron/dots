---
- name: Create base directories
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: directory
    mode: "{{ item.mode | default(omit) }}"
  loop: "{{ base_directories }}"
  loop_control:
    label: "{{ item.path }}"
